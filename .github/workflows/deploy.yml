name: "Deploy Codigo Facilito"

on:
  push:
    branches: ["main"] 

jobs:
  deploy-codigofacilito:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hacemos pull del code"
      - run: echo "Correr migraciones"
      - run: echo "Generar binario"
      - run: echo "Publicar codigo"