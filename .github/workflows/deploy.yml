name: Deploy to my server

on: push

jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo"
      - name: CAT
        run: cat hola.php 

  second:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first
    runs-on: ubuntu-latest
    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo Segunda vez"